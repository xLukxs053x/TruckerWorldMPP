services:
  discord-bot:
    build: .
    container_name: truckerworldmp-discord-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs

